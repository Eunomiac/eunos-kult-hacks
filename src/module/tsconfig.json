{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": [
      "fvtt-types",
      "jquery",
      "gsap"
    ],
    "paths": {
      // "k4lt": ["./@types/k4lt"]  // Add this to map the k4lt module
    },
    "noEmit": false,
    "emitDeclarationOnly": true,
    "outDir": ".typescript-build",
    "declarationMap": true,
    "composite": true
  },
  "include": [
    "**/*",
    // "./@types/**/*"  // Add this to include your type definitions
  ],
  "exclude": []
}
